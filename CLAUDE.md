# 작업 규칙

## 커밋
- 작업 하나가 끝나면 사용자가 따로 말하지 않아도 git commit까지 완료할 것
- 커밋 전 반드시: npx tsc(타입체크 통과) + 콘솔 오류 0건 확인
- 이 저장소는 브랜치 없이 master에 직접 커밋한다 (1인 로컬 저장소, 리모트 없음)
- 커밋 메시지는 무엇을 왜 바꿨는지 한국어로 간단히

## 데이터 수정 시
- README.md에 정리된 0번 원칙(하위 페이지 전부 훑고 판단) 및 1~9번 데이터 규칙을 항상 우선 적용
- 재실행 가능한 스크립트를 새로 만들 경우, 멱등성 3종 검증(문구 등장횟수 + 원문 완전일치 + 무손실) 후 커밋

## 개발 서버
- 개발 서버(npm run dev) 켜진 상태에서 npm run build 실행 금지 (.next 충돌)

## 보고
- 실제로 확인/검증한 것과, 확인 못 했지만 그대로 둔 부분을 구분해서 명확히 보고할 것 (과장 금지)